services:
  agribot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: telegram_agribot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_PATH=/app/data/agribot.db
